package ${packageName}

import com.naver.android.svc.core.SvcBaseCT

/**
 * @author ${USER}
 */
class ${className}CT(owner: ${className}Fragment, views: ${className}Views) : SvcBaseCT<${className}Fragment, ${className}Views>(owner, views) {

    override fun onCreated() {
    }
}