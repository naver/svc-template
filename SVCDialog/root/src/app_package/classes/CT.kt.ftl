package ${packageName}

import com.naver.android.svc.core.SvcCT

/**
 * @author ${USER}
 */
class ${className}CT(screen: ${className}Dialog, views: ${className}Views) : SvcCT<${className}Dialog, ${className}Views>(screen, views) {

    override fun onCreated() {
    }
}