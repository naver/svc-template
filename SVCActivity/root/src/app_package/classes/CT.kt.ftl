package ${packageName}

import com.naver.android.svc.core.SvcBaseCT

/**
 * @author ${USER}
 */
class ${className}CT(owner: ${className}Activity, views: ${className}Views) : SvcBaseCT<${className}Activity, ${className}Views>(owner, views) {

    override fun onCreated() {
    }
}