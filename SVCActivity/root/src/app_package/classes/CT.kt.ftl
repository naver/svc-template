package ${packageName}

import com.naver.android.svc.core.SvcCT

/**
 * @author ${USER}
 */
class ${className}CT(screen: ${className}Activity, views: ${className}Views) : SvcCT<${className}Activity, ${className}Views>(screen, views) {

    override fun onCreated() {
    }
}